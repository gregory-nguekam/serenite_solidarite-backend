server:
  port: 8080
  tomcat:
    max-part-count: 20

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/serenite_solidarite
    username: postgres
    password: admin
  mail:
    host: smtp.gmail.com
    port: 587
    username: gnguekam46@gmail.com
    password: chuisoabmvlejjzl
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  jpa:
    hibernate:
      ddl-auto: update 
      show-sql: true 
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

app:
  jwt:
    secret: "CHANGE_ME_TO_A_LONG_RANDOM_SECRET_32+CHARS"
    expiration-minutes: 15
  cors:
    allowed-origins: "http://localhost:5173"
  mail:
    from: ${MAIL_FROM:${spring.mail.username}}

